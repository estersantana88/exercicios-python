def misturar_cores_primarias(cor1, cor2):
    """Esta função recebe as cores como parâmetros"""
    cor1 = cor1.lower()
    cor2 = cor2.lower()
    
    mistura_de_cores = {
        ('vermelho', 'azul'): 'roxo',
        ('azul', 'vermelho'): 'roxo',
        ('vermelho', 'amarelo'): 'laranja',
        ('amarelo', 'vermelho'): 'laranja',
        ('azul', 'amarelo'): 'verde',
        ('amarelo', 'azul'): 'verde',
    }
    
    cores_primarias = ['azul', 'vermelho', 'amarelo']
    
    # ESTAS VERIFICAÇÕES ESTÃO DENTRO DA FUNÇÃO
    if cor1 not in cores_primarias:
        return f"'{cor1}' não é uma cor primária"
    
    if cor2 not in cores_primarias:
        return f"'{cor2}' não é uma cor primária"
    
    if cor1 == cor2:
        return f"Será {cor1} (mesma cor)"
    
    combinacao = (cor1, cor2)
    if combinacao in mistura_de_cores:
        return f"{cor1} + {cor2} = {mistura_de_cores[combinacao]}"
    
    return "Combinação não encontrada"

def main():
    """Função principal - AQUI ficam os inputs"""
    print("=== MISTURADOR DE CORES PRIMÁRIAS ===")
    print("Cores disponíveis: vermelho, azul, amarelo")
    print("Digite 'sair' para encerrar")
    
    while True:
        # AQUI cor1 e cor2 são criadas
        cor1 = input("\nEscolha sua primeira cor primária: ")
        
        if cor1.lower() == 'sair':
            break
        
        cor2 = input("Escolha outra cor primária: ")
        
        # AQUI chamamos a função passando cor1 e cor2
        resultado = misturar_cores_primarias(cor1, cor2)
        print(f"🎨 {resultado}")

# IMPORTANTE: Esta linha executa o programa
if __name__ == "__main__":
    main()